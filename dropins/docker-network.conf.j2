# Use the {{ name }} docker network
[Unit]
Requires=docker-network@{{ name }}.service
After=docker-network@{{ name }}.service

[Service]
Environment='DOCKER_NETWORK={{ name }}'
{% if ip is defined -%}
Environment='DOCKER_IP={{ ip }}'
{% endif -%}
